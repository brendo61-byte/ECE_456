These scripts creates a UDP packet and encrypt/decrypt data via a DES method. It will check to ensure that data send and received have
matching check sums. If a check sum test fails then the data is descarded - otherwise it is saved to the "output" file.

The two scripts require CL arguments (see lab for details).

Testest using python 3.7