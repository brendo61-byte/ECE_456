There are two parts to this lab:

1) Uses a UDP connection
2) Uses a TCP connection

To run the program source the venv in the root dir of this lab and then run the bash scripts has directed in the lab manual.

Note: Servers can only serve one client at a time